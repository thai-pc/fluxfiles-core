// Shared test secret (>= 32 bytes for firebase/php-jwt v7). Used to write the
// server's .env AND to mint matching tokens in the spec.
export const TEST_SECRET = 'pw-e2e-secret-key-0123456789abcdefgh';
export const PORT = 8099;
